let grid = 20;
let wide = 34 * grid;
let height = wide;

// Coulor palette
let colour = {
  W : '#ffffff',
  Y : '#f6e64b',
  R : '#b33025',
  B : '#2d59b5',
  G : '#d8d8d8',
};



function setup() {
  createCanvas(wide, height);
  noLoop();
}

function draw() {
  background(colour.W)


  
}